{
  "name": "console-snoop",
  "version": "0.1.1",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "babel index.js --out-dir dist -watch"
  },
  "author": "Derek Tor",
  "license": "Apache-2.0",
  "peerDependencies": {
    "sinon": "^2.0.0 || ^3.0.0 || ^4.0.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.14.0"
  }
}
